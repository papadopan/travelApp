import React from 'react';
import styled from 'styled-components';

const HomeWrapper = styled.div`
  margin-top: 10rem;
`;

const Home = () => {
  return <HomeWrapper>Antonios</HomeWrapper>;
};

export default Home;
